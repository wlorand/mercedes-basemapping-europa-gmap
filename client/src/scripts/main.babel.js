'use strict';

/** 
  * file: main.js
  */

function initMap() {

	console.log('here will still still be the gmap');

	var name = 'Will';
	console.log(name);
}

// Event Listeners
document.addEventListener('DOMContentLoaded', initMap);
